function numberZeroDigits(n){

    // Initialize result 
    var count = 0; 
  
    // Keep dividing n by powers of  
    // 5 and update count 
    for (var i = 5; n / i >= 1; i *= 5) 
    count += parseInt(n / i);
    return count; 
}
